Module Module1
    Sub Main()
        'Dim db As New DBManager()
        ''db.testConnection()

        'Dim format As String = "ID: {0,3}   Name: {1,-20}   Age: {2,3}   Gender: {3,-10}"

        ''For Each row As DataRow In db.getAllUsers()
        ''    Console.WriteLine(String.Format(format, row("u_id"), row("u_fname") & " " & row("u_lname"), row("u_age"), row("u_gender")))
        ''Next


        'Dim user As New User("George", "BBQ", 21, "Toaster")
        'db.AddNewUser(user)

        'db.DeleteUser(42)

        'For Each u As User In db.GetAllUsersList
        '    Console.WriteLine(String.Format(format, u.Id, u.Fname & " " & u.Lname, u.Age, u.Gender))
        'Next

        'Console.Read()
    End Sub
End Module
